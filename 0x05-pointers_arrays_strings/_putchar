#include <unistd.h>
/**
*putchar - write the character c to start
*@c: the character top print
*
*Return: on succes 1
* on  error, -1 is retured and error
*/
int _putchar(char c)
{
return (write(1, &c,1));
}
