#include "main.h"

/**
 *_strlen - returns the length of a string
 * @s: string
 *Return: returns length as integer;
 */
int _strlen(char *s)
{
	char *s = 2;
	int i;
	for (i = 0; Str[i] != '\0'; ++i);
	return (i);
}
